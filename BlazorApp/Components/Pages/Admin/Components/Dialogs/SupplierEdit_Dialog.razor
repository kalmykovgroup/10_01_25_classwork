@* <MudDialog @bind-Visible="isSupplierModalVisible" MaxWidth="MaxWidth.Small">
    <MudDialogTitle>@(selectedSupplier?.Id == Guid.Empty ? "Добавить поставщика" : "Редактировать поставщика")</MudDialogTitle>
    <MudDialogContent>
        <MudForm @ref="supplierForm" Valid="@supplierValid">
            <MudTextField @bind-Value="selectedSupplier.Name" Label="Название" Required="true" />
            <MudTextField @bind-Value="selectedSupplier.ContactEmail" Label="Email" />
            <MudTextField @bind-Value="selectedSupplier.ContactPhone" Label="Телефон" />
        </MudForm>
        @if (!string.IsNullOrEmpty(supplierErrorMessage))
        {
            <MudAlert Severity="Severity.Error" Class="mt-2">@supplierErrorMessage</MudAlert>
        }
    </MudDialogContent>
    <MudDialogActions>
        <MudButton Variant="Variant.Text" OnClick="CloseSupplierModal">Отмена</MudButton>
        <MudButton Variant="Variant.Filled" Color="Color.Primary" OnClick="SaveSupplier">Сохранить</MudButton>
    </MudDialogActions>
</MudDialog>
 *@